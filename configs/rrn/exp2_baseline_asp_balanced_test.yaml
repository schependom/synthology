defaults:
  - exp2_baseline_hpc
  - override data/dataset: asp_balanced_test_baseline
  - _self_

test:
  checkpoint_glob: "${oc.env:REPO_ROOT}/reports/experiment_runs/*/*/train_rrn/*/checkpoints/best-checkpoint-exp2-baseline*.ckpt"

logger:
  log_model: false
  name: "exp2_baseline_asp_balanced_test"
  tags: [exp2, baseline, hpc, asp_neutral_test, balanced]

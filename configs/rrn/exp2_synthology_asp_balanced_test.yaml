defaults:
  - exp2_synthology_hpc
  - override data/dataset: asp_balanced_test_synthology
  - _self_

test:
  checkpoint_glob: "${oc.env:REPO_ROOT}/reports/experiment_runs/*/*/train_rrn/*/checkpoints/best-checkpoint-exp2-synthology*.ckpt"

logger:
  log_model: false
  name: "exp2_synthology_asp_balanced_test"
  tags: [exp2, synthology, hpc, asp_neutral_test, balanced]

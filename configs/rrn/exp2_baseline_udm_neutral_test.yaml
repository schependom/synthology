defaults:
  - exp2_baseline_hpc
  - override data/dataset: udm_neutral_test_baseline
  - _self_

test:
  checkpoint_glob: "${oc.env:REPO_ROOT}/reports/experiment_runs/*/*/train_rrn/*/checkpoints/best-checkpoint-exp2-baseline*.ckpt"

logger:
  log_model: false
  name: "exp2_baseline_udm_neutral_test"
  tags: [exp2, baseline, hpc, udm_neutral_test]

defaults:
  - exp2_synthology_hpc
  - override data/dataset: udm_neutral_test_synthology
  - _self_

test:
  checkpoint_glob: "${oc.env:REPO_ROOT}/reports/experiment_runs/*/*/train_rrn/*/checkpoints/best-checkpoint-exp2-synthology*.ckpt"

logger:
  log_model: false
  name: "exp2_synthology_udm_neutral_test"
  tags: [exp2, synthology, hpc, udm_neutral_test]

universities: 20

baseline:
  reasoning_input_triple_cap: 1200
  abox_jena_heap_mb: 8192
  final_reasoning_input_triple_cap: 15000
  final_jena_profile: owl_mini
  # All canonical paper settings are already set in configs/owl2bench/config.yaml.
  # Only add genuine per-run overrides here; leave empty for paper runs.
  args: ""

synthology:
  args: ""

training:
  args: ""

report:
  baseline_path: ""
  synthology_path: ""
  use_balanced_synthology: true
  args: ""
  run_baseline_analysis: true

balance:
  baseline_dir: ""
  synthology_dir: ""
  output_dir: ""
  seed: 23

gold_test:
  source_test_dir: ""
  output_test_dir: ""

paper_visual_report:
  exp2_synth_targets: data/exp2/synthology/family_tree/train/targets.csv
  exp2_parity_summary: data/exp2/baseline/parity_runs/parity_loop_summary.json
  exp3_targets: data/owl2bench/output/owl2bench_20/train/targets.csv
  exp3_abox: data/owl2bench/output/raw/owl2bench_20/OWL2RL-5.owl
  exp3_inferred: data/exp3/baseline/owl2bench_20/inferred.nt
  out_dir: reports/paper
  args: ""

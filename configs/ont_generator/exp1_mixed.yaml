# Configuration for Experiment 1: Mixed Negative Sampling
# The README file also contains further details.

defaults:
  - exp1_base # override default config options
  - _self_

dataset:
  output_dir: "data/exp1/mixed"

neg_sampling:
  strategy: "mixed"
  corrupt_base_facts: true
  mixed_weights:
    proof_based: 0.5
    constrained: 0.5
    random: 0.0
    type_aware: 0.0

defaults:
  - config
  - _self_

dataset:
  output_dir: "data/exp1/test_set"
  n_train: 0
  n_val: 0

hydra:
  run:
    dir: "${dataset.output_dir}/hydra"

neg_sampling:
  strategy: "proof_based"
  corrupt_base_facts: true

generator:
  min_lcc_ratio: 0.6

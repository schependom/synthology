defaults:
  - config
  - _self_

dataset:
  output_dir: "data/exp1/random"
  n_test: 0

hydra:
  run:
    dir: "${dataset.output_dir}/hydra"

neg_sampling:
  strategy: "random"

generator:
  min_lcc_ratio: 0.6
